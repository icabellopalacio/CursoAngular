import { PipeSinTerminarPipe } from './pipe-sin-terminar.pipe';

describe('PipeSinTerminarPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeSinTerminarPipe();
    expect(pipe).toBeTruthy();
  });
});
